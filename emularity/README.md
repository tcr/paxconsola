To test out all emulator builds, first download all the files by running in the root directory:

```
make emulators
```

Then you can run

```
python -m http.server
```

In this directory to try out all emulators at once.
